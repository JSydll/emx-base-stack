header:
  version: 1

env:
  LOCAL_MIRROR_DIR: "../mirror"

local_conf_header:
  local-mirror: |
    SOURCE_MIRROR_URL ?= "file://${TOPDIR}/${LOCAL_MIRROR_DIR}"
    INHERIT += "own-mirrors"
    BB_GENERATE_MIRROR_TARBALLS = "1"